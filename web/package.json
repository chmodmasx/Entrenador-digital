{
  "name": "entrenador-digital-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/decode-nbs-hero.mjs",
    "dev": "vite",
    "prebuild": "node scripts/decode-nbs-hero.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^7.2.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
